We implemented JWT authentication using secure HttpOnly cookies instead of storing tokens in `localStorage`. This approach improves security by reducing exposure to XSS attacks, since HttpOnly cookies cannot be accessed directly from frontend JavaScript. The backend handles token issuance and cookie management, while the frontend relies on authenticated requests with credentials included.
